function map_ifcheck {
	if [ "$position" = "98" ]; then
		position="$last"
	fi
	if [ "$position" = "97" ]; then
		position="$last"
	fi
	if [ "$position" = "96" ]; then
		position="$last"
	fi
	if [ "$position" = "95" ]; then
		position="$last"
	fi
	if [ "$position" = "94" ]; then
		position="$last"
	fi
	if [ "$position" = "93" ]; then
		position="$last"
	fi
	if [ "$position" = "92" ]; then
		position="$last"
	fi
	if [ "$position" = "91" ]; then
		position="$last"
	fi
	if [ "$position" = "90" ]; then
		position="$last"
	fi
	if [ "$position" = "89" ]; then
		position="$last"
	fi
	if [ "$position" = "88" ]; then
		position="$last"
	fi
	if [ "$position" = "87" ]; then
		position="$last"
	fi
	if [ "$position" = "86" ]; then
		position="$last"
	fi
	if [ "$position" = "85" ]; then
		position="$last"
	fi
	if [ "$position" = "84" ]; then
		position="$last"
	fi
	if [ "$position" = "83" ]; then
		position="$last"
	fi
	if [ "$position" = "82" ]; then
		position="$last"
	fi
	if [ "$position" = "81" ]; then
		position="$last"
	fi
	if [ "$position" = "60" ]; then
		position="$last"
	fi
	if [ "$position" = "40" ]; then
		position="$last"
	fi
	if [ "$position" = "20" ]; then
		position="$last"
	fi
	if [ "$position" = "1" ]; then
		position="$last"
	fi
	if [ "$position" = "2" ]; then
		position="$last"
	fi
	if [ "$position" = "3" ]; then
		position="$last"
	fi
	if [ "$position" = "4" ]; then
		position="$last"
	fi
	if [ "$position" = "5" ]; then
		position="$last"
	fi
	if [ "$position" = "6" ]; then
		position="$last"
	fi
	if [ "$position" = "7" ]; then
		position="$last"
	fi
	if [ "$position" = "8" ]; then
		position="$last"
	fi
	if [ "$position" = "9" ]; then
		position="$last"
	fi
	if [ "$position" = "10" ]; then
		position="$last"
	fi
	if [ "$position" = "11" ]; then
		position="$last"
	fi
	if [ "$position" = "12" ]; then
		position="$last"
	fi
	if [ "$position" = "13" ]; then
		position="$last"
	fi
	if [ "$position" = "14" ]; then
		position="$last"
	fi
	if [ "$position" = "15" ]; then
		position="$last"
	fi
	if [ "$position" = "16" ]; then
		position="$last"
	fi
	if [ "$position" = "17" ]; then
		position="$last"
	fi
	if [ "$position" = "18" ]; then
		position="$last"
	fi
	if [ "$position" = "39" ]; then
		position="$last"
	fi
	if [ "$position" = "59" ]; then
		position="$last"
	fi
	if [ "$position" = "79" ]; then
		position="$last"
	fi
	if [ "$position" = "100" ]; then
		finish="True"
	fi
}
