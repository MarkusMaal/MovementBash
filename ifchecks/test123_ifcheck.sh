function map_ifcheck {
	if [ "$position" = "40" ]; then
		position="$last"
	fi
	if [ "$position" = "41" ]; then
		position="$last"
	fi
	if [ "$position" = "21" ]; then
		position="$last"
	fi
	if [ "$position" = "61" ]; then
		position="$last"
	fi
	if [ "$position" = "62" ]; then
		position="$last"
	fi
	if [ "$position" = "63" ]; then
		position="$last"
	fi
	if [ "$position" = "43" ]; then
		position="$last"
	fi
	if [ "$position" = "3" ]; then
		position="$last"
	fi
	if [ "$position" = "64" ]; then
		position="$last"
	fi
	if [ "$position" = "65" ]; then
		position="$last"
	fi
	if [ "$position" = "45" ]; then
		position="$last"
	fi
	if [ "$position" = "25" ]; then
		position="$last"
	fi
	if [ "$position" = "27" ]; then
		position="$last"
	fi
	if [ "$position" = "7" ]; then
		position="$last"
	fi
	if [ "$position" = "47" ]; then
		position="$last"
	fi
	if [ "$position" = "87" ]; then
		position="$last"
	fi
	if [ "$position" = "89" ]; then
		position="$last"
	fi
	if [ "$position" = "69" ]; then
		position="$last"
	fi
	if [ "$position" = "49" ]; then
		position="$last"
	fi
	if [ "$position" = "9" ]; then
		position="$last"
	fi
	if [ "$position" = "11" ]; then
		position="$last"
	fi
	if [ "$position" = "31" ]; then
		position="$last"
	fi
	if [ "$position" = "51" ]; then
		position="$last"
	fi
	if [ "$position" = "51" ]; then
		position="$last"
	fi
	if [ "$position" = "91" ]; then
		position="$last"
	fi
	if [ "$position" = "92" ]; then
		position="$last"
	fi
	if [ "$position" = "13" ]; then
		position="$last"
	fi
	if [ "$position" = "33" ]; then
		position="$last"
	fi
	if [ "$position" = "73" ]; then
		position="$last"
	fi
	if [ "$position" = "93" ]; then
		position="$last"
	fi
	if [ "$position" = "34" ]; then
		position="$last"
	fi
	if [ "$position" = "35" ]; then
		position="$last"
	fi
	if [ "$position" = "55" ]; then
		position="$last"
	fi
	if [ "$position" = "75" ]; then
		position="$last"
	fi
	if [ "$position" = "97" ]; then
		position="$last"
	fi
	if [ "$position" = "77" ]; then
		position="$last"
	fi
	if [ "$position" = "57" ]; then
		position="$last"
	fi
	if [ "$position" = "37" ]; then
		position="$last"
	fi
	if [ "$position" = "19" ]; then
		finish="True"
	fi
}
