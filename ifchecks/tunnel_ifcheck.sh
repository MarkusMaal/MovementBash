function map_ifcheck {
	if [ "$position" = "2" ]; then
		position="$last"
	fi
	if [ "$position" = "22" ]; then
		position="$last"
	fi
	if [ "$position" = "21" ]; then
		position="$last"
	fi
	if [ "$position" = "20" ]; then
		position="$last"
	fi
	if [ "$position" = "40" ]; then
		position="$last"
	fi
	if [ "$position" = "60" ]; then
		position="$last"
	fi
	if [ "$position" = "61" ]; then
		position="$last"
	fi
	if [ "$position" = "62" ]; then
		position="$last"
	fi
	if [ "$position" = "4" ]; then
		position="$last"
	fi
	if [ "$position" = "24" ]; then
		position="$last"
	fi
	if [ "$position" = "64" ]; then
		position="$last"
	fi
	if [ "$position" = "84" ]; then
		position="$last"
	fi
	if [ "$position" = "86" ]; then
		position="$last"
	fi
	if [ "$position" = "46" ]; then
		position="$last"
	fi
	if [ "$position" = "26" ]; then
		position="$last"
	fi
	if [ "$position" = "6" ]; then
		position="$last"
	fi
	if [ "$position" = "47" ]; then
		position="$last"
	fi
	if [ "$position" = "48" ]; then
		position="$last"
	fi
	if [ "$position" = "28" ]; then
		position="$last"
	fi
	if [ "$position" = "88" ]; then
		position="$last"
	fi
	if [ "$position" = "70" ]; then
		position="$last"
	fi
	if [ "$position" = "50" ]; then
		position="$last"
	fi
	if [ "$position" = "30" ]; then
		position="$last"
	fi
	if [ "$position" = "32" ]; then
		position="$last"
	fi
	if [ "$position" = "12" ]; then
		position="$last"
	fi
	if [ "$position" = "72" ]; then
		position="$last"
	fi
	if [ "$position" = "52" ]; then
		position="$last"
	fi
	if [ "$position" = "74" ]; then
		position="$last"
	fi
	if [ "$position" = "94" ]; then
		position="$last"
	fi
	if [ "$position" = "34" ]; then
		position="$last"
	fi
	if [ "$position" = "35" ]; then
		position="$last"
	fi
	if [ "$position" = "36" ]; then
		position="$last"
	fi
	if [ "$position" = "76" ]; then
		position="$last"
	fi
	if [ "$position" = "77" ]; then
		position="$last"
	fi
	if [ "$position" = "37" ]; then
		position="$last"
	fi
	if [ "$position" = "18" ]; then
		position="$last"
	fi
	if [ "$position" = "38" ]; then
		position="$last"
	fi
	if [ "$position" = "78" ]; then
		position="$last"
	fi
	if [ "$position" = "99" ]; then
		position="$last"
	fi
	if [ "$position" = "79" ]; then
		position="$last"
	fi
	if [ "$position" = "59" ]; then
		finish="True"
	fi
}
