function map_ifcheck {
	if [ "$position" = "80" ]; then
		position="$last"
	fi
	if [ "$position" = "60" ]; then
		position="$last"
	fi
	if [ "$position" = "40" ]; then
		position="$last"
	fi
	if [ "$position" = "20" ]; then
		position="$last"
	fi
	if [ "$position" = "0" ]; then
		position="$last"
	fi
	if [ "$position" = "1" ]; then
		position="$last"
	fi
	if [ "$position" = "2" ]; then
		position="$last"
	fi
	if [ "$position" = "4" ]; then
		position="$last"
	fi
	if [ "$position" = "5" ]; then
		position="$last"
	fi
	if [ "$position" = "6" ]; then
		position="$last"
	fi
	if [ "$position" = "7" ]; then
		position="$last"
	fi
	if [ "$position" = "8" ]; then
		position="$last"
	fi
	if [ "$position" = "9" ]; then
		position="$last"
	fi
	if [ "$position" = "10" ]; then
		position="$last"
	fi
	if [ "$position" = "11" ]; then
		position="$last"
	fi
	if [ "$position" = "12" ]; then
		position="$last"
	fi
	if [ "$position" = "13" ]; then
		position="$last"
	fi
	if [ "$position" = "14" ]; then
		position="$last"
	fi
	if [ "$position" = "15" ]; then
		position="$last"
	fi
	if [ "$position" = "16" ]; then
		position="$last"
	fi
	if [ "$position" = "17" ]; then
		position="$last"
	fi
	if [ "$position" = "18" ]; then
		position="$last"
	fi
	if [ "$position" = "39" ]; then
		position="$last"
	fi
	if [ "$position" = "59" ]; then
		position="$last"
	fi
	if [ "$position" = "79" ]; then
		position="$last"
	fi
	if [ "$position" = "99" ]; then
		position="$last"
	fi
	if [ "$position" = "77" ]; then
		position="$last"
	fi
	if [ "$position" = "57" ]; then
		position="$last"
	fi
	if [ "$position" = "76" ]; then
		position="$last"
	fi
	if [ "$position" = "75" ]; then
		position="$last"
	fi
	if [ "$position" = "74" ]; then
		position="$last"
	fi
	if [ "$position" = "73" ]; then
		position="$last"
	fi
	if [ "$position" = "72" ]; then
		position="$last"
	fi
	if [ "$position" = "71" ]; then
		position="$last"
	fi
	if [ "$position" = "70" ]; then
		position="$last"
	fi
	if [ "$position" = "69" ]; then
		position="$last"
	fi
	if [ "$position" = "68" ]; then
		position="$last"
	fi
	if [ "$position" = "67" ]; then
		position="$last"
	fi
	if [ "$position" = "66" ]; then
		position="$last"
	fi
	if [ "$position" = "65" ]; then
		position="$last"
	fi
	if [ "$position" = "64" ]; then
		position="$last"
	fi
	if [ "$position" = "63" ]; then
		position="$last"
	fi
	if [ "$position" = "62" ]; then
		position="$last"
	fi
	if [ "$position" = "42" ]; then
		position="$last"
	fi
	if [ "$position" = "3" ]; then
		finish="True"
	fi
}
