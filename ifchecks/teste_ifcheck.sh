function map_ifcheck {
	if [ "$position" = "77" ]; then
		position="$last"
	fi
	if [ "$position" = "97" ]; then
		position="$last"
	fi
	if [ "$position" = "58" ]; then
		position="$last"
	fi
	if [ "$position" = "59" ]; then
		position="$last"
	fi
	if [ "$position" = "33" ]; then
		position="$last"
	fi
	if [ "$position" = "13" ]; then
		position="$last"
	fi
	if [ "$position" = "52" ]; then
		position="$last"
	fi
	if [ "$position" = "51" ]; then
		position="$last"
	fi
	if [ "$position" = "50" ]; then
		position="$last"
	fi
	if [ "$position" = "49" ]; then
		position="$last"
	fi
	if [ "$position" = "48" ]; then
		position="$last"
	fi
	if [ "$position" = "27" ]; then
		position="$last"
	fi
	if [ "$position" = "7" ]; then
		position="$last"
	fi
	if [ "$position" = "63" ]; then
		position="$last"
	fi
	if [ "$position" = "60" ]; then
		position="$last"
	fi
	if [ "$position" = "81" ]; then
		position="$last"
	fi
	if [ "$position" = "82" ]; then
		position="$last"
	fi
	if [ "$position" = "42" ]; then
		position="$last"
	fi
	if [ "$position" = "41" ]; then
		position="$last"
	fi
	if [ "$position" = "99" ]; then
		finish="True"
	fi
	if [ "$position" = "62" ]; then
	position="28"
	elif [ "$position" = "32" ]; then
	position="78"
	fi
}
